# CHARTER.md — product-x

> **本文件由 AI 代人类起草（2026-07-30），是 product-x 仓库的唯一人类可编辑真源。**
> 人类应当逐条审读并按需改写；改写后请更新文末的 `last-human-edit` 日期。
> 所有卡片的 `charter` 字段必须映射到本文件下方"机器可读索引"里的编号，
> 否则 `gate/charter-hash` 直接红。
>
> **product-x 的特殊性**：它不是一个真实产品，它是"未来所有产品仓库的样板"。
> 因此本 CHARTER 的产品目标就是"成为一个正确、可复制、始终对齐 loop 最新实践的样板"。
> 真实产品仓库从 product-x 生成后，应当**整体替换本文件的 G/Q/U 段**（换成真实产品目标），
> 但**必须继承 N 段全部条目**（N 段是 LOOP 体系的红线，不随产品变化）。

---

## P 产品是什么（一句话）

product-x 是 LOOP 体系的**参考实现样板仓库**：它自身端到端跑通"接单→实现→异构验证→强模型验收→复现→合并"的全闭环，
并以最小、可复制的形态，向未来的产品仓库示范"一个正确接入 loop 控制面的产品仓应该长什么样"。

## U 用户与场景

- **U1 未来产品仓库的创建者（人类）**：用 product-x 作为 GitHub template 生成新产品仓，
  期望改动 ≤5 处（产品名、CHARTER 的 G/Q/U 段、`LOOP.yml` 的 lens 集合、CI 的语言栈、CODEOWNERS）即可开工。
- **U2 进仓干活的 AI 工作单元**：冷启动、无人类在场，只凭仓库内容就能判断"我该干什么、干到什么程度算完、谁来验我"。
- **U3 loop 控制面自身**：把 product-x 当作唯一的端到端集成测试床——loop 的任何机制变更，
  必须先在 product-x 上被证明可工作，才允许 fan-out 到真实产品仓。

---

## G 本季度目标（可判定）

### G1 端到端可用，而不只是"可工作"

> "可工作" = 每个部件单独跑得动。"可用" = 一个不了解本体系的人/AI，
> 按仓库内的说明从零开始，能不问任何人地走完一整轮并得到确定性结果。

判定：见 Q1、Q2。

### G2 门禁真实拦截，零假绿

> 名义上的 required check 必须真的能红。任何"探不到文件就 SKIP 且 exit 0"、
> "`|| true`"、"`set +e` 吞掉退出码"的模式，在本仓库一律视为缺陷而非权宜。

判定：见 Q3。

### G3 验证与验收独立可信

> 实现方不得自证。验证工与实现工必须异构（不同模型），且异构由机器强制而非文档约定；
> 强模型验收只产出可证伪断言，断言必须被独立复现才能进入修复。

判定：见 Q4、Q5。

### G4 作为样板可被复制，且复制品持续对齐最新实践

> 复制出去的产品仓不允许持有 loop 机制的副本，只允许持有对 loop 的 pin 引用。
> loop 发布新实践后，各产品仓自动收到升级 PR，并由确定性门禁校验其对齐度。

判定：见 Q6、Q7。

---

## Q 质量底线（数值，可机器判定）

| ID | 底线 | 判定方法 |
|---|---|---|
| Q1 | 冷启动到领到第一张卡 ≤1 分钟（wall clock），全程零人类输入 | 干净沙盒计时，证据入 VERDICT |
| Q2 | 端到端闭环成功率 ≥80%（连续 10 张卡中 ≥8 张走通 impl→verify→review→merge） | `bench/` 重放 + conductor 统计 |
| Q3 | 假绿数 = 0：CI 中不存在任何"失败被吞成绿"的步骤；且每个 required check 都有一条"故意让它红"的负向测试 | `gate/no-fake-green` + 负向测试卡 |
| Q4 | 100% 的 standard/critical 卡，其 verify 模型 ≠ impl 模型，且该约束由 CI 强制而非仅由文档声明 | `gate/heterogeneity` 读 VERDICT 的 `verifier_model` 比对 |
| Q5 | 进入修复的强模型 claim，100% 先有一条 `REPRODUCED` 的独立复现记录 | `gate/claim-confirmed`；未复现的 claim 不允许开修复卡 |
| Q6 | 产品仓的 `LOOP.yml` 声明的 `loop_version` 落后主干 ≤2 个 tag 且 ≤30 天 | `gate/loop-conformance` |
| Q7 | 产品仓内 loop 机制文件（gates/lenses/conductor/prompts）副本数 = 0；全部经 reusable workflow 引用 | `gate/loop-conformance` 扫目录 |
| Q8 | 单卡 diff 不超过 `policy.yml` 的 `execute.max_diff_lines` 上限 | `gate/diff-size` |

---

## N 绝不做（红线；AI 提了自动驳回。**复制到真实产品仓时必须原样继承**）

- **N1 不让任何开源件接管跨卡调度。** 单卡执行器可以外包（opencode / Codex CLI / Copilot CLI），
  跨卡的派单、租约、回收、依赖放行永远由 loop 自己的 conductor 持有。
- **N2 不做无外部可观测收益的重构。** "更优雅""更现代""更符合最佳实践"都不构成理由；
  必须能指出一个外部可观测的指标变化。
- **N3 不自动修正 GitHub branch ruleset / 组织权限 / secrets。**
  检测到漂移一律开 Incident 并停在那里等人类，永不自动写回。
- **N4 不从非官方源安装任何外部依赖。** 每个外部依赖必须登记进 `UPSTREAM.yaml`，
  钉到完整 SHA，过冷静期，且带 `degrade_path`。
- **N5 不把假绿当作权宜之计。** 宁可让流水线红着、让波次停住，也不允许用
  `|| true` / `set +e` / 探不到就 SKIP 的方式把失败伪装成成功。
  临时不可用的能力必须显式失败并说明原因。
- **N6 不允许实现方自证。** impl 不得给自己的卡产 VERDICT；
  评审模型不得给自己的 claim 做复现判定；任何"同一模型既做又验"的路径一律作废。
- **N7 不把模型的论断当作事实。** 强模型（含最强模型）的输出只是"待检验的输入"，
  不可证伪的论断一律拒收，未被独立复现的论断不得触发任何代码改动。
- **N8 不在 product-x 样板里塞真实产品逻辑。** 样板只示范 LOOP 体系；
  一旦开始承载真实业务，它就不再是样板。
- **N9 不在产品仓复制 loop 的机制文件。** 只允许 pin + reusable workflow 引用。
- **N10 不把高权限凭证放进仓库级 secret。** 每个 token 按最小权限拆分，
  能用 `GITHUB_TOKEN` 的绝不用 PAT，能用 GitHub App 的绝不用 PAT。

---

## GRIPE BOX（人类吐槽入口）

人类对本体系的任何不满，直接写进本仓库 issue #1（标题 `Gripe Box`）的评论，不需要任何格式。
planner 在每次开波次时必须先消化 GRIPE BOX 中未处理的条目，优先级高于 Finding 与债务。

---

<!-- ============================================================ -->
<!-- 机器可读索引：conductor/materialize.py 的 load_charter_ids() -->
<!-- 用正则 ^([GNUQ]\d+)\s 逐行解析下列条目。新增 G/N/Q/U 时       -->
<!-- 必须同步在此登记，否则引用该编号的卡无法通过 gate/charter。   -->
<!-- ============================================================ -->

## 索引（machine-readable，勿删）

G1 端到端可用，而不只是可工作
G2 门禁真实拦截，零假绿
G3 验证与验收独立可信
G4 作为样板可被复制且持续对齐最新实践
N1 不让任何开源件接管跨卡调度
N2 不做无外部可观测收益的重构
N3 不自动修正 ruleset/权限/secrets
N4 不从非官方源安装依赖
N5 不把假绿当作权宜之计
N6 不允许实现方自证
N7 不把模型的论断当作事实
N8 不在样板里塞真实产品逻辑
N9 不在产品仓复制 loop 机制文件
N10 不把高权限凭证放进仓库级 secret
Q1 冷启动到领卡 ≤1 分钟
Q2 端到端闭环成功率 ≥80%
Q3 假绿数为零且每个门禁有负向测试
Q4 verify 模型与 impl 模型异构由 CI 强制
Q5 进入修复的 claim 必先有 REPRODUCED 记录
Q6 LOOP.yml 落后主干 ≤2 tag 且 ≤30 天
Q7 产品仓 loop 机制副本数为零
Q8 单卡 diff 不超过 policy 上限
U1 未来产品仓创建者
U2 进仓干活的 AI 工作单元
U3 loop 控制面自身

---

<!-- last-human-edit: 2026-07-31 — 已按 Wave 13 验收标准审读并认可。 -->
